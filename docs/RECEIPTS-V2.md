# Receipts v2 — from tamper-evident to non-repudiable

Status: **SHIPPED** (Python suite, 2026-09-24, lane branch `receipts-v2-envelope`).
Original design note below, kept verbatim; the acceptance sketch's four tests
are implemented in `tests/test_receipts_v2.py` (`test_acceptance_*`), all
passing. The Rust/TS suite extension of acceptance test 1 is deferred: the
cross-language contract ships as `vectors/signed_receipt_vectors.json`
(byte-identity pins, pin-time cross-checked against PyNaCl and the official
blake3 package) instead of as live sibling suites.
Date: 2026-09-21 (design) / 2026-09-24 (build)

## What shipped (2026-09-24)

- `jev_quilt/blake3.py` — stdlib-only BLAKE3, single-chunk path (receipt
  messages are ~150 bytes; >1024-byte inputs are a loud `ValueError`, not a
  silent wrong answer). 400-case fuzz vs the official blake3 package,
  byte-exact.
- `jev_quilt/ed25519.py` — stdlib-only RFC 8032 Ed25519 (pure-python
  reference arithmetic). 120 seeds byte-exact vs PyNaCl, both directions.
- `jev_quilt/signed_receipts.py` — the envelope: `canonical_bytes` (exactly
  the sha() preimage, pipe-joined UTF-8), `chain_head_hex` (fnv1a-64 over the
  tip's hex — substrate-neutral), `seal`/`verify_envelope` plus low-level
  `seal_bytes`/`verify_bytes` for foreign/sibling receipts (twist-engine's
  canonical form is its own substrate's rule: `body||hash` UTF-8), key
  rotation (`rotation_payload`/`sign_rotation` — the NEW key signs the old
  key's retirement), and refusal polarity: verification returns structured
  verdicts (`unknown_signer` / `bad_signature` / `wrong_chain_head` /
  `malformed:...`) so callers book `v2-refused` rows — refusals are ledger
  rows, never exceptions, never silence.
- `tests/fixtures/twist_engine_sibling_rows.json` — REAL sibling rows:
  generated by running twist-engine's shipped `jev-receipts.mjs`
  (SuperInstance/twist-engine @ 23047cb7) and re-verified in-test by porting
  the sibling's chain rule exactly (hv accumulates across BOTH rounds — no
  reset; code points, not UTF-8 bytes; the fixture test caught a wrong first
  port). Family cross-repo contract, exercised with real rows per doctrine.
- `vectors/signed_receipt_vectors.json` — the machine-readable cross-language
  contract any port must reproduce byte-for-byte.

Positioning (cite map: `research/2026-09-24-receipts-v2-mutmem-convergence.md`
in the cocapn workspace): MutMem (arXiv:2608.02843, 2026-08-03) converges on
no-fork predecessor binding, quantized weights, Ed25519 dual-path, signed
revisable labels, gate-at-write, and an honesty clause — all CITED, none
claimed novel. The v2 claims that remain ours, per that map: (1) refusal
polarity as first-class ledger rows; (2) occurred/recorded time-window split
(family-level; jev-quilt receipts are tick-sequenced and the envelope adds no
wall clock — the split is delegated to family envelopes that carry one);
(3) the family envelope cut (core family-owned fields + producer
 context; exercised here with real twist-engine rows); (4) nursery
 contrast retention (sibling-family concern); (5) interchange vs
 authorization layering (the
 signature is over `canonical_bytes || chain_head`, never over the transport
 JSON — `to_canonical_json` is for moving envelopes, not for verifying them).

## Why v2 exists

The current receipt layer is FNV-1a hash-chained: each Receipt's sha() walks the
chain and re-derives payload_hash from the retained payload (see PR #3's repair —
the text, not just the hash field, is chained). This gives us **tamper evidence**
inside one process and one ledger: any edit anywhere upstream breaks re-derivation.

Two honest limits remain:

1. **No non-repudiation.** FNV-1a is not a signature. Anyone holding the ledger can
   recompute the whole chain. A receipt proves *the ledger is intact*, not *who
   wrote it*. Cross-node, "the bookkeeper says so" is not a proof.
2. **Legacy-weak primitive.** FNV-1a-64 is a fast non-cryptographic hash, chosen for
   exact-rational reproducibility across Python/Rust/TS (café Δ 日本語 →
   0x024a555471370b18d pinned in three suites). Corroboration from the frontier:
   inchwormz/agent-receipts ships BLAKE3 + Ed25519 hash-chained receipts — the
   fleet's receipt doctrine is right, our primitive reads legacy next to it.

## v2 shape: verify-only signature layer

Keep the FNV-1a chain exactly as-is (it is the reproducible, cross-language spine —
the pinned vectors stay valid). Add a **signature envelope** around it:

```
SignedReceipt = {
  receipt:      <existing Receipt, unchanged>,
  chain_head:   fnv1a-64 of the chain tip this receipt seals,
  signer:       <node/agent identity key id>,
  signature:    Ed25519.sign(signer_sk, BLAKE3(receipt.canonical_bytes || chain_head))
}
```

Rules:

- **Sign, don't replace.** The FNV-1a chain remains the in-ledger truth; signatures
  are an envelope a verifier can strip. Old verifiers still work.
- **Verify-only receipts for cross-node claims.** A node that only ever verifies
  never needs a signing key — verification is permissionless, minting is not.
- **One key per node identity, not per receipt.** Key rotation books a receipt of
  its own (new key signs the old key's retirement) or the chain forks socially.
- **Non-repudiation is a cross-node property only.** Within one WAL the chain
  already settles disputes; signatures earn their cost exactly where the chain
  cannot: two nodes, two bookkeepers, one argument.

## What v2 does NOT do (honest gaps)

- No timestamp authority — wall-clock smuggling stays a documented gap (see the
  floor-as-clock essay: time claims must derive from the ledger, not ride along).
- No key distribution — signer identity resolution is out of scope; v2 signs
  key IDs, it does not prove key IDs belong to anyone.
- No revocation beyond retirement receipts — a stolen key writes until rotation.
- Receipt bloat: every signed receipt is ~96 bytes heavier; WAL compactification
  (aef8b40) must decide whether signatures survive folding or stay on the tip.

## Acceptance sketch (when built) — SUPERSEDED 2026-09-24

The "wait for a real two-node dispute" deferral is superseded: a dedicated
build lane ran the sketch. Test 4's "two-node dispute" exists as
`test_acceptance_cross_node_negative` — a real negative test with two
distinct keys, not a scenario we waited for.

1. Same receipt, three suites, signature byte-identical across Python/Rust/TS
   (canonical bytes discipline, same as the café pin). — Python live; Rust/TS
   contract = vectors file (deviation recorded above).
2. Tamper test: flip one payload bit → chain re-derivation fails AND signature
   verification fails, independently. — `test_acceptance_tamper_chain_and_signature_fail_independently`, PASS.
3. Strip test: verifier that ignores envelopes reproduces today's behavior
   bit-for-bit (backward compat is a test, not a hope). — `test_acceptance_strip_backward_compat`, PASS.
4. Cross-node test: node A signs, node B verifies, node B cannot mint an A
   receipt with its own key (negative test). — `test_acceptance_cross_node_negative`, PASS.

## Why now, why not now

Corroboration arrived (agent-receipts) and the cross-language floor is proven
(three suites, one vector) — the design can be written honestly. The build waits
for a real two-node dispute; until then the FNV-1a chain is the right cost.

*Update (2026-09-22):* the write-path provenance triptych (POSITIONING.md §4 —
MemGhost 2607.05189, forensic signatures 2606.30566, csoai harness w/
CVE-2026-24301) puts a preemption clock on the *namespaced* part of this
doc: the receipts lane is being framed externally, and the one component
nobody else ships — hash-chained namespaced write-lineage — is the piece
to land before the vocabulary commoditizes. Citation obligation recorded;
no design change.
